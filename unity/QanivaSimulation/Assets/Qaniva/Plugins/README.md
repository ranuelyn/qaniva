# Plugins

`ClinicalCore/` is populated by `scripts/sync-clinical-core-to-unity.sh` with the
compiled `Qaniva.Clinical.Core.dll` and its dependencies. That folder is
git-ignored — the DLL is a build artifact of `clinical-core/`, not source.

Do not hand-edit or commit DLLs here.
